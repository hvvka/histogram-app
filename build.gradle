version 'unspecified'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile project(':stats-process')
    compile project(':stats-gui')

    testCompile group: 'junit', name: 'junit', version: '4.12'
}
